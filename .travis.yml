language: php
php:
  - 7.3

before_script:
  - cp .env.travis .env
  - mysql -e 'create database test;'
  - composer self-update
  - composer install
  - chmod -R 777 storage

script: 
  - ./vendor/bin/phpunit

notifications:
  emails:
    - narishige7@gmail.com